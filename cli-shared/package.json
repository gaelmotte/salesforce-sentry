{
  "name": "@salesforce-sentry/cli-shared",
  "version": "0.3.0",
  "description": "Shared utilities, migration runner, and codemods for salesforce-sentry CLI tools",
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "utils/",
    "migrations/",
    "runner.js",
    "state.js"
  ],
  "scripts": {
    "test": "node --test 'test/*.test.js'",
    "release": "npm view $npm_package_name@$npm_package_version version 2>/dev/null && echo 'already published, skipping' || npm publish"
  },
  "dependencies": {
    "diff": "^5.2.0",
    "fast-xml-parser": "^4.3.6",
    "jscodeshift": "^0.16.1",
    "picocolors": "^1.1.0",
    "prompts": "^2.4.2"
  }
}
