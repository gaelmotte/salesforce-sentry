# Flow Faults Integration

Attaches Flow interview context to fault events — including the interview GUID, faulting element name, and Flow definition ID. No parameters.

```apex
new SentryFlowFaultIntegration()
```

See [Using the SDK in Your Package](./usage) for how to wire the Capture Sentry Event element into your Flow fault paths.
