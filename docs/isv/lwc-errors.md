# LWC Errors Integration

Attaches the LWC component stack trace to errors captured from Lightning Web Components. No parameters.

```apex
new SentryLWCErrorIntegration()
```

See [Using the SDK in Your Package](./usage) for how to instrument LWC components with `SentryMixin` and `SentryBoundaryMixin`.
